<!DOCTYPE html>
<html lang="<%= typeof lang !== 'undefined' ? lang : 'en' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof t !== 'undefined' ? t('auth.loginTitle') : 'Panel Login' %> | C³ CELERITY</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="login-page">
    <div class="login-container">
        <div class="login-box">
            <div class="login-header">
                <span class="logo-icon large">⚡</span>
                <h1>C³ CELERITY</h1>
                <a href="https://t.me/First_VPN_Click_bot" target="_blank" style="background: linear-gradient(135deg, var(--accent) 0%, #a855f7 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; text-decoration: none;">by Click Connect</a>
            </div>
            
            <!-- Language Switcher -->
            <div class="login-lang-switcher">
                <a href="?lang=ru" class="lang-btn <%= typeof lang !== 'undefined' && lang === 'ru' ? 'active' : '' %>">RU</a>
                <a href="?lang=en" class="lang-btn <%= typeof lang !== 'undefined' && lang === 'en' ? 'active' : '' %>">EN</a>
            </div>
            
            <% if (error) { %>
            <div class="alert alert-error"><%= error %></div>
            <% } %>
            
            <form method="POST" action="/panel/login" class="login-form">
                <div class="form-group">
                    <label for="username"><%= typeof t !== 'undefined' ? t('auth.login') : 'Login' %></label>
                    <input type="text" id="username" name="username" 
                           placeholder="<%= typeof t !== 'undefined' ? t('auth.enterLogin') : 'Enter login' %>" required autofocus>
                </div>
                <div class="form-group">
                    <label for="password"><%= typeof t !== 'undefined' ? t('auth.password') : 'Password' %></label>
                    <input type="password" id="password" name="password" 
                           placeholder="<%= typeof t !== 'undefined' ? t('auth.enterPassword') : 'Enter password' %>" required>
                </div>
                <button type="submit" class="btn btn-primary btn-block"><%= typeof t !== 'undefined' ? t('auth.loginButton') : 'Sign In' %></button>
            </form>
        </div>
    </div>
    
    <style>
    .login-lang-switcher {
        display: flex;
        justify-content: center;
        gap: 8px;
        margin-bottom: 20px;
    }
    .login-lang-switcher .lang-btn {
        padding: 6px 16px;
        font-size: 12px;
        font-weight: 500;
        color: var(--text-secondary);
        text-decoration: none;
        border-radius: 6px;
        background: var(--bg-tertiary);
        transition: 0.15s ease;
    }
    .login-lang-switcher .lang-btn:hover {
        color: var(--text-primary);
        background: var(--bg-hover);
    }
    .login-lang-switcher .lang-btn.active {
        color: var(--text-primary);
        background: var(--accent);
    }
    </style>
</body>
</html>
